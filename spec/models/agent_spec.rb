# -*- coding: utf-8 -*-
require File.join(File.dirname(__FILE__), '..', '/spec_helper')

describe Agent do
  it "should indicate if an agent is an admin or not" do
  end

  it "should validate the password only when it is created" do
  end

  it "should populate the hashd_password and the salt when the password is set" do
  end

  it "gives the next action for a fiche depending on teh status" do
    end

  it "indicate if the edit button is to be displayed" do
  end
end
