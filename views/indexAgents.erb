<div class="row">
  <div class="span5 offset2"><h3>Les agents</h3></div>
</div>
<div class="span5">
  <table class="table table-striped table-condensed">
    <thead>
      <tr>
	<th>Nom</th>
	<th>Email</th>
	<th>Rôle</th>
	<th></th>
      </tr>
    </thead>
    <tbody>
      <% if @agents.count == 0 %>
      <tr><td colspan="4" class="center">Il n'y a pas d'agent</td></tr>
      <% else %>
      <% @agents.each do |agent|%>
      <tr class="ficheRow">
	<td><%=agent.nom %></td>
	<td class="center"><%=agent.email %></td>
	<td class="center"><%=agent.role.nom %></td>
	<td>
	  <a href="/agent/<%=agent.id %>" title="Modifier"><i class="icon-pencil"></i></a>
	</td>
      </tr>
      <% end %>
      <% end %>
    </tbody>
  </table>
  <button class="btn btn-info lien" type="button" data-target="/agent">Ajouter un agent</button>
</div>
